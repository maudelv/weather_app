{
    "repository": {
        "type": "git",
        "url": "git+https://github.com/phoenixframework/phoenix.git"
    },
    "private": true,
    "scripts": {
        "deploy": "tailwind -i css/app.css -o ../priv/static/assets/app.css --minify",
        "watch": "tailwind -i css/app.css -o ../priv/static/assets/app.css --watch"
    },
    "devDependencies": {
        "esbuild": "^0.17.11",
        "postcss": "^8.4.21",
        "postcss-loader": "^7.0.2",
        "tailwindcss": "^3.2.7"
    }
}